var WebApi = require("../"), api, bool, TextProcessor, optionTypes;

describe("TextProcessor", function () {
    
    beforeEach(function() {
        TextProcessor = WebApi.TextProcessor;
    });

    it("loaded", function() {
        expect(TextProcessor).toBeDefined();
    });
    
    it('should have main inteface methods: createOptions', function() {
       
    });

});